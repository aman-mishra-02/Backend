<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= foundProduct.name %>
    </title>
</head>

<body>
    <h1>Edit <%= foundProduct.name %>
    </h1>
    <form action="/products/<%= foundProduct.id %>?_method=PUT" method="post">
        <input type="text" name="name" id="name" placeholder="Product Name" value="<%= foundProduct.name %>">
        <br>
        <label for="price">Price: &dollar;</label>
        <input type="number" name="price" id="price" placeholder="Price" value="<%= foundProduct.price%>">
        <br>
        <label for=" category">Category: </label>
        <select name="category" id="category">
            <% for(let category of categories){ %>
                <option value="<%=category%>" <%=foundProduct.category===category ? 'selected' : '' %>><%=category%>
                </option>
                <% } %>
        </select>
        <br>
        <button>Edit</button>
    </form>

    <a href="/products/<%= foundProduct._id %>"><button>Cancel</button></a>
</body>

</html>